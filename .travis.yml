language: go

go:
- "1.11.x"

go_import_path: github.com/sylr/prometheus-azure-exporter

env:
- GO111MODULE=on

script:
- gofmt -d -e -l . && git diff --exit-code
- go mod tidy
- go test -v ./...
- go build
